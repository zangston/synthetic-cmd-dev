# for each fits file pair:
    # open pair of Baraffe and corresponding low-mass Baraffe files for a given log-age
    # open and save fits files to two separate tables, create new table
    
    # for each file in low-mass baraffe fits file:
        # append all rows in low-mass file up to mass limit
    
    # switch to normal fits file
    # for each row in merged model fits file:
        # append row

    # save to new file with the same naming convention
